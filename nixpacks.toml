[phases.setup]
nixPkgs = ["bun"]

[phases.caddy]
nixPkgs = []
cmds = []

[phases.install]
cmds = ["bun install --frozen-lockfile"]
cacheDirectories = ["node_modules"]

[phases.build]
cmds = ["bun install --frozen-lockfile", "bun run catalog", "bun run build"]

[start]
cmd = "bun run server/index.ts"
